{
  "name": "easy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd packages && cd react-frontend && npm install && npm start && cd .. cd ..",
    "dev": "cd packages && cd express-backend && npm install && npm run dev && cd .. cd .."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "create-react-app": "^5.0.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
